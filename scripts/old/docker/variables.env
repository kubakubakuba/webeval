#################################
#SAMPLE ENVIRONMENT VARIABLES
#################################

#these three should have the same content
POSTGRES_DB=qtrvsim_web_eval
DB_NAME=qtrvsim_web_eval
DB_DATABASE=qtrvsim_web_eval

#postgres root user name and password
POSTGRES_USER=postgres
POSTGRES_PASSWORD=YourDesiredRootPasswordHere

#works with docker-compose, change only when you have an external database
DB_HOST=db
DB_PORT=5432

#name, and password for the webeval tables
DB_USER=qtrvsim
DB_PASSWORD=YourDesiredRootPasswordHere

#flask app secret key
SECRET_KEY=abcdef0123456789

#mail configuration
MAIL_SERVER=smtp.seznam.cz
MAIL_PORT=465
MAIL_USE_TLS=False
MAIL_USE_SSL=True
MAIL_USERNAME=qtrvsim@server.host
MAIL_PASSWORD=YourDesiredEmailPasswordHere
MAIL_DEFAULT_SENDER=qtrvsim@server.host

#base URL for email links and API documentation
BASE_URL=https://eval.comparch.edu.cvut.cz

#configurable directories for templates and task files
#these paths should be relative to the docker host, and will be mounted into the containers
TEMPLATES_DIR=./.docker/S_templates
TASKS_DIR=./.docker/tasks

#port configuration
HTTP_PORT=80  # Main HTTP port for nginx reverse proxy
WEB_PORT=8000  # Internal Flask app port (not exposed externally in production)
